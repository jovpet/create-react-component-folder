{{#if scss }}
{{~#if scssinclude}}
{{#each scssinclude}}
@import '{{this}}';
{{/each}}
{{~/if}}
{{/if}}

{{#if hyphenatedcss~}}
.{{ hyphenatedname }} {
  //css styles
}
{{else}}
.{{ name }} {
  //css styles
}
{{~/if}};
