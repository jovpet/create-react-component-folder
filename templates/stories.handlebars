import React from 'react';
{{#if typescript}}import { Story, Meta } from '@storybook/react/types-6-0';{{/if}}
import {{#if export~}} { {{ name }} } {{else~}} {{ name }} {{~/if}} from './index';
{{#if typescript~}}
  import { {{ name }}{{patternName}}Props } from './
  {{~#if uppercase~}}
    {{~name}}{{patternFile}}
    {{~else~}}
    {{~nameLowerCase}}{{patternFile}}
  {{~/if}}';
{{/if}}

export default {
component: {{~#if uppercase~}} {{~name}} {{~else~}} {{~nameLowerCase}} {{~/if}},
title: '{{~#if uppercase~}} {{~name}} {{~else~}} {{~nameLowerCase}} {{~/if}}'
}{{#if typescript}} as Meta{{/if}};

const Template{{#if typescript}}: Story<{{~name}}{{patternName}}Props>{{/if}} = args => <{{name}} {...args} />;

export const Default = Template.bind({});
Default.args = {
};
